---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "netmaker_ext_client Resource - netmaker"
subcategory: ""
description: |-
  Creates a Netmaker ext client (remote-access WireGuard config) attached to an existing ingress gateway node, and deploys it onto a target machine over SSH (installs wireguard-tools if needed, brings up the interface/tunnel).
---

# netmaker_ext_client (Resource)

Creates a Netmaker ext client (remote-access WireGuard config) attached to an existing ingress gateway node, and deploys it onto a target machine over SSH (installs wireguard-tools if needed, brings up the interface/tunnel).



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `gateway_node_id` (String) ID of an existing ingress gateway node in the given network (see netmaker_node's is_ingress_gateway). Changing this forces recreation.
- `mode` (Attributes) Deploy mechanism. SSH is currently the only one supported. (see [below for nested schema](#nestedatt--mode))
- `network` (String) Network the gateway node belongs to. Changing this forces recreation.

### Optional

- `device_name` (String)
- `dns` (String)
- `enabled` (Boolean) Whether the ext client is enabled. Defaults to true.
- `extra_allowed_ips` (List of String)
- `post_down` (String)
- `post_up` (String)
- `tags` (List of String) IDs of tags to apply to this ext client — reference a netmaker_tag's `id`. Each tag must already exist and be in `network` (Netmaker doesn't auto-create tags).

### Read-Only

- `address` (String)
- `address6` (String)
- `id` (String) Server-assigned ext client ID.
- `private_key` (String, Sensitive)
- `public_key` (String)

<a id="nestedatt--mode"></a>
### Nested Schema for `mode`

Required:

- `ssh` (Attributes) Connect and deploy over SSH. (see [below for nested schema](#nestedatt--mode--ssh))

<a id="nestedatt--mode--ssh"></a>
### Nested Schema for `mode.ssh`

Required:

- `host_ip` (String) IP address (or hostname) of the target machine.
- `username` (String) SSH username.

Optional:

- `host_port` (Number) SSH port. Defaults to 22.
- `password` (String, Sensitive) SSH password. Only used if neither private_key nor private_key_path is set.
- `private_key` (String, Sensitive) PEM-encoded SSH private key content. Mutually exclusive with private_key_path; use exactly one of private_key/private_key_path, or password.
- `private_key_path` (String) Path to a PEM-encoded SSH private key file, read from the machine running Terraform. Mutually exclusive with private_key; use exactly one of private_key/private_key_path, or password.
